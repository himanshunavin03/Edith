export const environment = {
  production: false,
  /**
   * Base URL of the EDITH backend. In dev, Angular runs on :4200 and the
   * backend on :3000. For iPhone testing over HTTPS/LAN, point this at the
   * machine's LAN HTTPS URL (see README "iPhone Safari testing").
   */
  apiBaseUrl: 'http://localhost:3000/api',
};
